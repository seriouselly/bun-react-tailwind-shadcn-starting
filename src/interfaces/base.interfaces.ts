export interface MetaInterface {
    totalItems: number;
    itemCount: number;
    itemPerPage: number;
    totalPage: number;
    currentPage: number;
}

export interface LinksInterface {
    first: string;
    prev: string;
    next: string;
    last: string;
}